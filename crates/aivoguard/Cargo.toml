[package]
name = "aivoguard"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description.workspace = true
readme = "../../README.md"
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

# This package hosts Gate-1 M01 and Gate-2 M02. It remains free of network,
# database, and LLM dependencies. M02 is read-only over M01 outputs.

[dependencies]
# None. Keep the Gate-1 kernel dependency-free (std only).

[lints]
workspace = true
