[package]
name = "aivoguard"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description.workspace = true
readme = "../../README.md"
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

# This package hosts Gate-1 M01, Gate-2 M02, and Gate-3 M03. It remains free of
# network, database, and LLM dependencies. M02/M03 are read-only over M01
# outputs (M03 orchestrates only).

[dependencies]
# None. Keep the Gate-1 kernel dependency-free (std only).

[lints]
workspace = true
